#include <vector>
#include <iostream>

class Solution {
public:
    // 是否可以：求出所有路径所有路径数量后，求出至少通过一个障碍物的路径？相减？
    int uniquePathsWithObstacles(const std::vector<std::vector<int>>& obstacleGrid) {

    }
};