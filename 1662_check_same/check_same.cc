#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include <unordered_map>
#include <unordered_set>

using namespace std;

/**
 * C++ 版本不用自己写了，O(1) 空间复杂度也很简单
 * 设置一个 word index 一个 字母 index 即可，字母 index 到当前长度后就更新 word index
 * runtime: /
 * memory:  /
 */
class Solution {
public:
    bool arrayStringsAreEqual(vector<string>& word1, vector<string>& word2) {
    }
};

int main(int argc, char** argv) {
    Solution sol;

    return 0;
}