#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

/**
 * 我当然可以暴力做，模拟法，但也太暴力了
 * runtime: 
 * memory:  
 */
class Solution {
public:
    vector<int> findPattern(vector<vector<int>>& board, vector<string>& pattern) {
        
    }
};

int main(int argc, char** argv) {
    Solution sol;

    return 0;
}