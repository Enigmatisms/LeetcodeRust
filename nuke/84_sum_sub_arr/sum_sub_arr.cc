#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include <unordered_map>
#include <unordered_set>

using namespace std;

/**
 * runtime:
 * memory:
 */
class Solution {
public:
    int sumSubarr(vector<int>& nums) {
    }
};

int main(int argc, char** argv) {
    Solution sol;

    return 0;
}