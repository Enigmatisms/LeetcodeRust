#include <iostream>
#include <vector>
#include <math.h>
#include <numeric>
#include <array>
#include <algorithm>

using namespace std;

/**
 * 写不出来啊，太菜了
 * 感觉和杨辉三角变体（三方向）有关系，但边界很难处理
 * 可能要在线对分母进行重新估计（边界的概率是1/2，非边界移动的概率是1/3）
 */

int main(int argc, char** argv) {
    
    return 0;
}